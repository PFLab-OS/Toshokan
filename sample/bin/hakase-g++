#!/bin/sh
ARGS="$*"
PROJECT_ROOT=$(cd "${PROJECT_ROOT:=${PWD}}" && pwd)
CMD="docker run -it --rm -v ${PROJECT_ROOT}:${PROJECT_ROOT} -w ${PROJECT_ROOT} livadk/toshokan_build_hakase g++ ${ARGS}"
${CMD}
