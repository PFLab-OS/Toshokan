rules:
  - template: hakase_bin
    path_extract_vars:
      - target
      - depends
      - clean_targets
    variables:
      options: "-c"
      target: test.o
      depends:
        - test.cc
      clean_targets:
        - "*.o"
        - "*.d"
