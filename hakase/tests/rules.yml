rules:
  - template: hakase_bin
    path_extract_vars:
      - clean_targets
    variables:
      cflags: $(HAKASE_CFLAGS) -c
      target: test.o
      depends:
        - test.cc
      clean_targets:
        - "*.o"
        - "*.d"
