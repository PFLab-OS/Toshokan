rules:
  - template: hakase_bin
    path_extract_vars:
      - target
      - depends
      - clean_targets
    variables:
      options: "-c"
      target: elf_loader.o
      depends:
        - hakase.cc
      clean_targets:
        - "*.o"
        - "*.d"
