rules:
  - template: hakase_bin
    path_extract_vars:
      - clean_targets
    variables:
      cflags: $(HAKASE_CFLAGS) -c
      target: elf_loader.o
      depends:
        - hakase.cc
      clean_targets:
        - "*.o"
        - "*.d"
